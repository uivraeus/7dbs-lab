# Example of connecting to MonoDB from a Python app

## Pre-req

```shell
python -m venv venv
source venv/bin/activate
pip install pymongo
```

or just (in new shell if package already installed):

```shell
source venv/bin/activate
```

## Run

```shell
python main.py
```
